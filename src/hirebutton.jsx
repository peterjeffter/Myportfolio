import react from "react";

const Hirebutton = ({className}) => {
    return (
     <button className={`hirebutton ${className}`} >
        Connect
     </button>
    );
  };
  
  export default Hirebutton;